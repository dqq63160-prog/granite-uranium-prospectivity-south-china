# Cross-task coupling workflow

This workflow compares the saved prospectivity and granite-classification OOF
results. It evaluates shared-feature attribution, group-level associations,
permutation tests, and sensitivity analyses without refitting the two models.

Inputs are the canonical numerical files in `../../results/`. Outputs are written
to `../../results/coupling/`.
